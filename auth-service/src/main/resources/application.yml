spring:
  cloud:
    config:
      uri: http://localhost:8888 # -> config-server-git'ın yayın yaptıgı port ${configserver_uri}
  config:
    import: "configserver:"
  application:
    name: auth-service-application